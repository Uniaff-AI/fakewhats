---
import { COPY } from '../content/copy';
import { siteConfig } from '../config/site-config';
const { popup } = COPY;
---

<div id="returnVisitPopup" class="return-visit-popup-backdrop" role="dialog" aria-modal="true" aria-label="Возврат к видео" style="display: none;">
  <div class="return-visit-popup">
    <div class="vsl-container">
      <div class="vsl-overlay">
        <div class="vsl-vignette"></div>
        <video id="returnVisitVideo" src={siteConfig.images.videos.chatVsl} controls playsinline preload="none" controlsList="nodownload nofullscreen noremoteplayback"></video>
        
        <!-- Крестик для закрытия VSL -->
        <button id="returnVisitVslCloseBtn" style="position: absolute; top: 10px; right: 10px; background: rgba(0, 0, 0, 0.7); color: white; border: none; border-radius: 50%; width: 40px; height: 40px; cursor: pointer; z-index: 8; font-size: 18px; pointer-events: auto;">✕</button>
        

        
        <!-- Попап на определенной минуте VSL (скрыт изначально) -->
        <div id="returnVisitVslMidPopup" class="return-visit-vsl-mid-popup" style="display: none;">
          <!-- Зеленая плашка сверху -->
          <div class="return-visit-vsl-mid-banner" onclick="showOrderFormModal();" style="cursor: pointer;">
            <div class="return-visit-vsl-mid-vignette"></div>
            <div>{siteConfig.texts.popups.vslMidPopup.title}</div>
            <div style="font-size: 14px; margin-top: 5px;">{siteConfig.texts.popups.vslMidPopup.subtitle}</div>
          </div>
        </div>
        
        <!-- Текст поверх видео -->
        <div class="vsl-text-overlay">
          <div class="popup-text">
            <h3>{siteConfig.texts.popups.returnPopup.title}</h3>
            <p class="consequence">{siteConfig.texts.popups.returnPopup.consequence}</p>
            <p class="solution">{siteConfig.texts.popups.returnPopup.solution}</p>
            <p class="highlight">{siteConfig.texts.popups.returnPopup.highlight}</p>
            <p class="urgency">{siteConfig.texts.popups.returnPopup.urgency}</p>
          </div>
          <div class="popup-buttons">
            <button id="continueWatchingVisit" class="btn-primary">{siteConfig.texts.popups.returnPopup.continueButton}</button>
            <button id="textVersionVisit" class="btn-secondary">{siteConfig.texts.buttons.restartVideo}</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  const returnVisitPopup = document.getElementById('returnVisitPopup');
  const returnVisitVideo = document.getElementById('returnVisitVideo');
  const continueVisitBtn = document.getElementById('continueWatchingVisit');
  const restartVisitBtn = document.getElementById('textVersionVisit');

  // Загружаем сохраненное время видео из localStorage
  let savedVideoTime = 0;
  const savedTimeFromStorage = localStorage.getItem('savedVideoTime');
  if (savedTimeFromStorage) {
    savedVideoTime = parseFloat(savedTimeFromStorage);
  }

  function showReturnVisitPopup() {
    returnVisitPopup.style.display = 'flex';
    if (returnVisitVideo) {
      // Устанавливаем время видео с сохраненного момента
      returnVisitVideo.currentTime = savedVideoTime;
      
      // Запускаем автовоспроизведение с обходом блокировки
      startAutoplayWithBypass(returnVisitVideo);
      
      // Настраиваем попап на определенной минуте для этого видео
      console.log('🎬 Setting up popup for returnVisitVideo');
      if (window.setupVslTimePopup) {
        console.log('🎬 Calling window.setupVslTimePopup(returnVisitVideo)...');
        window.setupVslTimePopup(returnVisitVideo);
      } else {
        console.error('❌ window.setupVslTimePopup is not defined!');
      }

      // Открываем форму заказа когда видео закончилось
      returnVisitVideo.addEventListener('ended', function() {
        console.log('🎬 Return visit video ended, showing order form');
        if (window.showOrderFormModal) {
          window.showOrderFormModal();
        }
      });
    }
  }

  function hideReturnVisitPopup() {
    // Сохраняем текущее время видео перед закрытием
    if (returnVisitVideo) {
      saveVideoTime();
    }
    
    returnVisitPopup.style.display = 'none';
    if (returnVisitVideo) {
      returnVisitVideo.pause();
    }
  }



  function saveVideoTime() {
    if (returnVisitVideo && returnVisitVideo.currentTime > 0) {
      const currentTime = returnVisitVideo.currentTime;
      localStorage.setItem('savedVideoTime', currentTime.toString());
      
      const minutes = Math.floor(currentTime / 60);
      const seconds = Math.floor(currentTime % 60);
    }
  }

  // Функция показа попапа на определенной минуте VSL
  function showReturnVisitVslMidPopup() {
    console.log('🎯 Showing return visit VSL mid popup');
    const popup = document.getElementById('returnVisitVslMidPopup');
    console.log('🎯 Popup element found:', !!popup);
    if (popup) {
      console.log('✅ Return visit popup element found, showing...');
      console.log('🎯 Popup current display:', popup.style.display);
      console.log('🎯 Popup current z-index:', popup.style.zIndex);
      
      // Принудительно показываем попап
      popup.style.display = 'block';
      popup.style.opacity = '1';
      popup.style.visibility = 'visible';
      popup.style.zIndex = '1000';
      
      console.log('🎯 Popup display after change:', popup.style.display);
      console.log('🎯 Popup z-index after change:', popup.style.zIndex);
      console.log('✅ Return visit popup should now be visible');
      
      // Проверяем, что баннер тоже виден
      const banner = popup.querySelector('.return-visit-vsl-mid-banner');
      if (banner) {
        console.log('✅ Return visit banner element found');
        banner.style.zIndex = '1001';
        console.log('🎯 Return visit banner z-index set to:', banner.style.zIndex);
      } else {
        console.error('❌ Return visit banner element not found!');
      }
    } else {
      console.error('❌ Return visit popup element not found! returnVisitVslMidPopup does not exist in DOM');
      
      // Проверим все элементы с похожими ID
      const allElements = document.querySelectorAll('[id*="popup"]');
      console.log('🎯 All elements with "popup" in ID:', Array.from(allElements).map(el => el.id));
    }
  }

  // Функция показа формы заказа
  function showOrderFormModal() {
    const orderFormModal = document.getElementById('orderFormModal');
    if (orderFormModal) {
      orderFormModal.style.display = 'flex';
      setTimeout(() => {
        orderFormModal.classList.add('show');
      }, 10);
    }
  }



  // Обработчик для крестика в ReturnVisitPopup
  const returnVisitVslCloseBtn = document.getElementById('returnVisitVslCloseBtn');
  returnVisitVslCloseBtn?.addEventListener('click', () => {
    // Показываем попап выбора
    if (window.showVslCloseChoicePopup) {
      window.showVslCloseChoicePopup();
    }
  });

  // Функция для обхода блокировки автовоспроизведения
  function startAutoplayWithBypass(video) {
    if (!video) return;
    
    // Стратегия 1: Пробуем с отключенным звуком (работает в большинстве браузеров)
    video.muted = true;
    video.play().then(() => {
      console.log('✅ Return visit video started playing (muted)');
      // Включаем звук сразу
      video.muted = false;
      console.log('🔊 Sound enabled immediately');
    }).catch((error) => {
      console.log('❌ Muted autoplay failed:', error.name);
      
      // Стратегия 2: Пробуем с задержкой
      setTimeout(() => {
        video.muted = true;
        video.play().then(() => {
          console.log('✅ Return visit video started playing (delayed muted)');
          // Включаем звук сразу
          video.muted = false;
          console.log('🔊 Sound enabled immediately (delayed)');
        }).catch((error2) => {
          console.log('❌ Delayed autoplay failed:', error2.name);
          
          // Стратегия 3: Ждем взаимодействия пользователя
          setupUserInteractionAutoplay(video);
        });
      }, 500);
    });
  }

  // Функция для запуска видео после взаимодействия пользователя
  function setupUserInteractionAutoplay(video) {
    let hasUserInteracted = false;
    
    // Создаем невидимую кнопку воспроизведения
    const playButton = document.createElement('div');
    playButton.style.cssText = `
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: transparent;
      cursor: pointer;
      z-index: 1000;
    `;
    playButton.innerHTML = `
      <div style="
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 20px 40px;
        border-radius: 10px;
        font-size: 18px;
        text-align: center;
        pointer-events: none;
      ">
        ▶️ Нажмите для воспроизведения
      </div>
    `;
    
    playButton.onclick = () => {
      if (!hasUserInteracted) {
        hasUserInteracted = true;
        playButton.remove();
        video.muted = false;
        video.play().then(() => {
          console.log('✅ Return visit video started after user interaction');
        }).catch((error) => {
          console.error('❌ Failed to play after interaction:', error);
        });
      }
    };
    
    // Добавляем обработчики для различных типов взаимодействия
    const interactionEvents = ['click', 'touchstart', 'keydown', 'mousedown'];
    interactionEvents.forEach(event => {
      playButton.addEventListener(event, () => {
        if (!hasUserInteracted) {
          hasUserInteracted = true;
          playButton.remove();
          video.muted = false;
          video.play().then(() => {
            console.log('✅ Return visit video started after user interaction');
          }).catch((error) => {
            console.error('❌ Failed to play after interaction:', error);
          });
        }
      });
    });
    
    returnVisitPopup.appendChild(playButton);
  }

  // Экспортируем функции для использования в других компонентах
  window.showReturnVisitPopup = showReturnVisitPopup;
  window.hideReturnVisitPopup = hideReturnVisitPopup;
  window.saveVideoTime = saveVideoTime;
  window.showReturnVisitVslMidPopup = showReturnVisitVslMidPopup;
  window.showOrderFormModal = showOrderFormModal;



  continueVisitBtn?.addEventListener('click', () => {
    // Сохраняем время перед скрытием элементов
    if (returnVisitVideo) {
      saveVideoTime();
    }
    
    // Скрываем надписи, кнопки и виньетку
    const textOverlay = document.querySelector('.vsl-text-overlay');
    const vignette = document.querySelector('.vsl-vignette');
    
    if (textOverlay) {
      textOverlay.style.display = 'none';
    }
    if (vignette) {
      vignette.style.display = 'none';
    }
    
    // Убираем курсор pointer с видео и возобновляем воспроизведение
    if (returnVisitVideo) {
      returnVisitVideo.style.cursor = 'default';
      
      // Возобновляем воспроизведение видео
      if (returnVisitVideo.paused) {
        returnVisitVideo.play().then(() => {
          console.log('▶️ Return visit video resumed from pause');
        }).catch((error) => {
          console.log('❌ Failed to resume return visit video:', error.name);
          // Если автоплей заблокирован, используем обход
          startAutoplayWithBypass(returnVisitVideo);
        });
      }
    }
  });

  restartVisitBtn?.addEventListener('click', () => {
    if (returnVisitVideo) {
      // Сбрасываем время видео на начало
      returnVisitVideo.currentTime = 0;
      
      // Очищаем сохраненное время
      savedVideoTime = 0;
      localStorage.removeItem('savedVideoTime');
      
      // Запускаем воспроизведение с начала используя обход блокировки
      startAutoplayWithBypass(returnVisitVideo);
      
      // Настраиваем попап на определенной минуте для видео, которое начинается с начала
      if (window.setupVslTimePopup) {
        window.setupVslTimePopup(returnVisitVideo);
      }
    }
    
    // Скрываем надписи, кнопки и виньетку (как при клике на "Продолжить")
    const textOverlay = document.querySelector('.vsl-text-overlay');
    const vignette = document.querySelector('.vsl-vignette');
    
    if (textOverlay) {
      textOverlay.style.display = 'none';
    }
    if (vignette) {
      vignette.style.display = 'none';
    }
    
    // Убираем курсор pointer с видео
    if (returnVisitVideo) {
      returnVisitVideo.style.cursor = 'default';
    }
  });

  // Обработчик клика на фон попапа
  returnVisitPopup?.addEventListener('click', (e) => {
    if (e.target === returnVisitPopup) {
      hideReturnVisitPopup();
      // Показываем домонетку при попытке закрыть попап
      if (window.showDomonetkaForm) {
        window.showDomonetkaForm();
      }
    }
  });

  // Обработчик клика на видео
  returnVisitVideo?.addEventListener('click', () => {
    // Просто сохраняем время, НЕ показываем домонетку
  });

  // Настройки видео (перемотка разрешена)
  if (returnVisitVideo) {
    // Отключаем только контекстное меню
    returnVisitVideo.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      return false;
    });
    
    // Перемотка теперь разрешена - убираем все блокировки
    console.log('✅ Video seeking enabled for returnVisitVideo');
  }

  // Сохраняем время видео при попытке ухода со страницы
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      saveVideoTime();
    }
  });

  // Периодически сохраняем время видео каждые 10 секунд во время воспроизведения
  setInterval(() => {
    if (returnVisitVideo && !returnVisitVideo.paused && returnVisitVideo.currentTime > 0) {
      saveVideoTime();
    }
  }, 10000); // Каждые 10 секунд
</script>

<style>
  .return-visit-popup-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.7);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 100;
  }

  .return-visit-popup {
    background: #fff;
    border-radius: 16px;
    overflow: hidden;
    max-width: 420px;
    max-height: 80vh;
    width: 90vw;
    height: 70vh;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  .vsl-container {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-overlay {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 2;
  }

  .vsl-overlay video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Исправление обрезания видео на мобильных устройствах */
  @media (max-width: 768px) {
    .vsl-overlay video {
      object-fit: contain; /* Показываем видео полностью без обрезания */
      background: #000; /* Черный фон для пустых областей */
    }
    
    .return-visit-popup {
      height: 60vh; /* Уменьшаем высоту на мобильных */
    }
  }

  @media (max-width: 480px) {
    .return-visit-popup {
      height: 50vh; /* Еще меньше на маленьких экранах */
    }
  }

  .vsl-text-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: rgba(0, 0, 0, 0.4);
    z-index: 3;
  }

  .popup-text {
    text-align: center;
    margin-bottom: 20px;
  }

  .popup-text h3 {
    margin: 0 0 8px 0;
    font-size: 20px;
    font-weight: 700;
    color: #ff4444;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-text p {
    margin: 0 0 6px 0;
    font-size: 14px;
    line-height: 1.3;
    color: #fff;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .consequence {
    font-weight: 600;
    color: #ff4444;
  }

  .solution {
    font-weight: 600;
    color: #fff;
    font-size: 16px;
  }

  .highlight {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 16px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .urgency {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 14px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-buttons {
    display: flex;
    flex-direction: column;
    gap: 12px;
    width: 100%;
    max-width: 250px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #ff6b35, #ff4444);
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    box-shadow: 0 4px 12px rgba(255, 107, 53, 0.4);
    text-transform: uppercase;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #e55a2b, #e53935);
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(255, 107, 53, 0.6);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.9);
    color: #333;
    border: 2px solid rgba(255, 255, 255, 0.3);
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    backdrop-filter: blur(10px);
    text-transform: uppercase;
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 1);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }

  /* Стили для попапа на определенной минуте VSL */
  .return-visit-vsl-mid-popup {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
    pointer-events: none;
    background: transparent; /* Убираем затемнение видео */
  }

  .return-visit-vsl-mid-banner {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    background: #4CAF50;
    color: white;
    padding: 15px;
    text-align: center;
    font-weight: bold;
    font-size: 16px;
    z-index: 11;
    pointer-events: auto;
    position: relative;
  }

  .return-visit-vsl-mid-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 1;
  }

  /* Элементы управления видео теперь видны */
  video {
    /* Стили для видимых элементов управления */
  }
</style>
