---
import { COPY } from '../content/copy';
import { siteConfig } from '../config/site-config';
const { popup } = COPY;
---

<!-- Попап возврата с кнопками (без VSL) -->
<div id="returnVisitPopup" class="return-visit-popup-backdrop" role="dialog" aria-modal="true" aria-label="Возврат к видео" style="display: none;">
  <div class="return-visit-popup">
    <div class="vsl-text-overlay">
      <div class="popup-text">
        <h3>{siteConfig.texts.popups.returnPopup.title}</h3>
        <p class="consequence">{siteConfig.texts.popups.returnPopup.consequence}</p>
        <p class="solution">{siteConfig.texts.popups.returnPopup.solution}</p>
        <p class="highlight">{siteConfig.texts.popups.returnPopup.highlight}</p>
        <p class="urgency">{siteConfig.texts.popups.returnPopup.urgency}</p>
      </div>
      <div class="popup-buttons">
        <button id="continueWatchingVisit" class="btn-primary">{siteConfig.texts.popups.returnPopup.continueButton}</button>
        <button id="textVersionVisit" class="btn-secondary">{siteConfig.texts.buttons.restartVideo}</button>
      </div>
    </div>
  </div>
</div>

<script>
  const returnVisitPopup = document.getElementById('returnVisitPopup');
  const continueVisitBtn = document.getElementById('continueWatchingVisit');
  const restartVisitBtn = document.getElementById('textVersionVisit');



  // Загружаем сохраненное время видео из localStorage
  let savedVideoTime = 0;
  const savedTimeFromStorage = localStorage.getItem('savedVideoTime');
  if (savedTimeFromStorage !== null) {
    savedVideoTime = parseFloat(savedTimeFromStorage);
    console.log('🎬 ReturnVisitPopup: Loaded saved video time:', savedVideoTime, 'seconds');
  } else {
    console.log('🎬 ReturnVisitPopup: No saved video time found');
  }

  function showReturnVisitPopup() {
    console.log('🎬 showReturnVisitPopup called - showing popup only');
    
    if (!returnVisitPopup) {
      console.log('❌ returnVisitPopup element not found');
      return;
    }
    
    console.log('✅ Showing return visit popup');
    returnVisitPopup.style.display = 'flex';
  }

  function hideReturnVisitPopup() {
    console.log('🎬 Hiding return visit popup');
    returnVisitPopup.style.display = 'none';
  }





  // Функция показа попапа на определенной минуте VSL
  function showReturnVisitVslMidPopup() {
    const popup = document.getElementById('returnVisitVslMidPopup');
    if (popup) {
      // Принудительно показываем попап
      popup.style.display = 'block';
      popup.style.opacity = '1';
      popup.style.visibility = 'visible';
      popup.style.zIndex = '1000';
      
      // Проверяем, что баннер тоже виден
      const banner = popup.querySelector('.return-visit-vsl-mid-banner');
      if (banner) {
        banner.style.zIndex = '1001';
      }
    }
  }

  // Функция показа формы заказа
  function showOrderFormModal() {
    const orderFormModal = document.getElementById('orderFormModal');
    if (orderFormModal) {
      orderFormModal.style.display = 'flex';
      setTimeout(() => {
        orderFormModal.classList.add('show');
      }, 10);
    }
  }







  // Экспортируем функции для использования в других компонентах
  window.showReturnVisitPopup = showReturnVisitPopup;
  window.hideReturnVisitPopup = hideReturnVisitPopup;
  window.showOrderFormModal = showOrderFormModal;



  continueVisitBtn?.addEventListener('click', () => {
    console.log('🎬 Continue watching button clicked');
    
    // Скрываем попап
    hideReturnVisitPopup();
    
    // Запускаем полноэкранный VSL с сохраненного времени
    if (window.showFullscreenPlayer) {
      window.showFullscreenPlayer();
    }
  });

  restartVisitBtn?.addEventListener('click', () => {
    console.log('🎬 Restart video button clicked');
    
    // Очищаем сохраненное время
    localStorage.removeItem('savedVideoTime');
    
    // Сбрасываем время в полноэкранном VSL
    const fullscreenVideo = document.getElementById('fullscreenVideo');
    if (fullscreenVideo) {
      fullscreenVideo.currentTime = 0;
      console.log('🎬 Reset fullscreen video time to 0');
    }
    
    // Скрываем попап
    hideReturnVisitPopup();
    
    // Запускаем полноэкранный VSL с начала
    if (window.showFullscreenPlayer) {
      window.showFullscreenPlayer();
    }
  });

  // Обработчик клика на фон попапа
  returnVisitPopup?.addEventListener('click', (e) => {
    console.log('🎬 Click detected on popup, target:', e.target, 'currentTarget:', e.currentTarget);
    
    // Проверяем, что клик был по фону (не по кнопкам или тексту)
    if (e.target === returnVisitPopup || e.target.classList.contains('return-visit-popup')) {
      console.log('🎬 Clicked on popup backdrop, showing domonetka');
      hideReturnVisitPopup();
      
      // Показываем домонетку при закрытии попапа
      if (window.showDomonetkaForm) {
        window.showDomonetkaForm();
      }
    } else {
      console.log('🎬 Click was on child element, not backdrop');
    }
  });

  // Обработчик клика на контейнер попапа
  const returnVisitPopupContainer = returnVisitPopup?.querySelector('.return-visit-popup');
  returnVisitPopupContainer?.addEventListener('click', (e) => {
    console.log('🎬 Click detected on popup container, target:', e.target);
    
    // Проверяем, что клик был по контейнеру (не по кнопкам или тексту)
    if (e.target === returnVisitPopupContainer || e.target.classList.contains('vsl-text-overlay')) {
      console.log('🎬 Clicked on popup container, showing domonetka');
      hideReturnVisitPopup();
      
      // Показываем домонетку при закрытии попапа
      if (window.showDomonetkaForm) {
        window.showDomonetkaForm();
      }
    } else {
      console.log('🎬 Click was on child element of container, not container itself');
    }
  });

  // Настройки видео (перемотка разрешена)
  if (returnVisitVideo) {
    // Отключаем только контекстное меню
    returnVisitVideo.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      return false;
    });
    
    // Перемотка теперь разрешена - убираем все блокировки
    console.log('✅ Video seeking enabled for returnVisitVideo');
  }

  // Сохраняем время видео при попытке ухода со страницы
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      saveVideoTime();
    }
  });

  // Периодически сохраняем время видео каждые 10 секунд во время воспроизведения
  setInterval(() => {
    if (returnVisitVideo && !returnVisitVideo.paused && returnVisitVideo.currentTime > 0) {
      saveVideoTime();
    }
  }, 10000); // Каждые 10 секунд
</script>

<style>
  .return-visit-popup-backdrop {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.7);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 1001; /* Выше чем fullscreen-player (z-index: 1000) */
  }

  .return-visit-popup {
    background: #000;
    border-radius: 0;
    overflow: hidden;
    max-width: 100vw;
    max-height: 100vh;
    width: 100vw;
    height: 100vh;
    box-shadow: none;
  }

  .vsl-container {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-overlay {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 2;
  }

  .vsl-overlay video {
    width: 100%;
    height: 100%;
    object-fit: contain;
    background: #000;
  }

  /* Исправление обрезания видео на мобильных устройствах */
  @media (max-width: 768px) {
    .vsl-overlay video {
      object-fit: contain; /* Показываем видео полностью без обрезания */
      background: #000; /* Черный фон для пустых областей */
    }
    
    .return-visit-popup {
      height: 100vh; /* Полная высота на мобильных */
      width: 100vw; /* Полная ширина на мобильных */
      max-width: 100vw;
      max-height: 100vh;
    }
  }

  @media (max-width: 480px) {
    .return-visit-popup {
      height: 100vh; /* Полная высота на маленьких экранах */
      width: 100vw; /* Полная ширина на маленьких экранах */
      max-width: 100vw;
      max-height: 100vh;
    }
  }

  .vsl-text-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: rgba(0, 0, 0, 0.4);
    z-index: 3;
  }

  .popup-text {
    text-align: center;
    margin-bottom: 20px;
  }

  .popup-text h3 {
    margin: 0 0 8px 0;
    font-size: 20px;
    font-weight: 700;
    color: #ff4444;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-text p {
    margin: 0 0 6px 0;
    font-size: 14px;
    line-height: 1.3;
    color: #fff;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .consequence {
    font-weight: 600;
    color: #ff4444;
  }

  .solution {
    font-weight: 600;
    color: #fff;
    font-size: 16px;
  }

  .highlight {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 16px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .urgency {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 14px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-buttons {
    display: flex;
    flex-direction: column;
    gap: 12px;
    width: 100%;
    max-width: 250px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #ff6b35, #ff4444);
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    box-shadow: 0 4px 12px rgba(255, 107, 53, 0.4);
    text-transform: uppercase;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #e55a2b, #e53935);
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(255, 107, 53, 0.6);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.9);
    color: #333;
    border: 2px solid rgba(255, 255, 255, 0.3);
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    backdrop-filter: blur(10px);
    text-transform: uppercase;
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 1);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }

  /* Стили для попапа на определенной минуте VSL */
  .return-visit-vsl-mid-popup {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
    pointer-events: none;
    background: transparent; /* Убираем затемнение видео */
  }

  .return-visit-vsl-mid-banner {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    background: #4CAF50;
    color: white;
    padding: 15px;
    text-align: center;
    font-weight: bold;
    font-size: 16px;
    z-index: 11;
    pointer-events: auto;
    position: relative;
  }

  .return-visit-vsl-mid-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 1;
  }

  /* Элементы управления видео теперь видны */
  video {
    /* Стили для видимых элементов управления */
  }
</style>
