---
import { COPY } from '../content/copy';
import { siteConfig } from '../config/site-config';
const { popup } = COPY;
---

<div id="returnPopup" class="return-popup-backdrop" role="dialog" aria-modal="true" aria-label="Возврат к видео" style="display: none;">
  <div class="return-popup">
    <div class="vsl-container">
      <div class="vsl-overlay">

        <div class="video-wrapper">
          <video id="returnVslVideo" src={siteConfig.images.videos.chatVsl} playsinline preload="metadata" controls webkit-playsinline x-webkit-airplay="allow" x5-video-player-type="h5" x5-video-player-fullscreen="false" x5-video-orientation="portraint"></video>
        </div>
        
        <!-- Виньетка для затемнения -->
        <div class="vsl-vignette"></div>
        
        <!-- Крестик для закрытия VSL -->
        <button id="returnVslCloseBtn" style="position: absolute; top: 10px; right: 10px; background: rgba(0, 0, 0, 0.7); color: white; border: none; border-radius: 50%; width: 40px; height: 40px; cursor: pointer; z-index: 20; font-size: 18px; pointer-events: auto;">✕</button>
        
        <!-- Текстовый оверлей с кнопками -->
        <div class="vsl-text-overlay">
          <div class="popup-text">
            <h3>{siteConfig.texts.popups.returnPopup.title}</h3>
            <p class="consequence">{siteConfig.texts.popups.returnPopup.consequence}</p>
            <p class="solution">{siteConfig.texts.popups.returnPopup.solution}</p>
            <p class="highlight">{siteConfig.texts.popups.returnPopup.highlight}</p>
            <p class="urgency">{siteConfig.texts.popups.returnPopup.urgency}</p>
          </div>
          <div class="popup-buttons">
            <button id="continueWatching" class="btn-primary">{siteConfig.texts.popups.returnPopup.continueButton}</button>
            <button id="textVersion" class="btn-secondary">{siteConfig.texts.buttons.restartVideo}</button>
          </div>
        </div>
        

        
        <!-- Попап на определенной минуте VSL (скрыт изначально) -->
        <div id="returnVslMidPopup" class="return-vsl-mid-popup" style="display: none;">
          <!-- Зеленая плашка сверху -->
          <div class="return-vsl-mid-banner" onclick="showOrderFormModal();" style="cursor: pointer;">
            <div class="return-vsl-mid-vignette"></div>
            <div>{siteConfig.texts.popups.vslMidPopup.title}</div>
            <div style="font-size: 14px; margin-top: 5px;">{siteConfig.texts.popups.vslMidPopup.subtitle}</div>
          </div>
        </div>
        

      </div>
    </div>
  </div>
</div>

<script>
  const returnPopup = document.getElementById('returnPopup');
  const returnVideo = document.getElementById('returnVslVideo');
  const continueBtn = document.getElementById('continueWatching');
  const restartBtn = document.getElementById('textVersion');

  // console.log('🎬 ReturnPopup elements initialized:', {
  //   returnPopup: !!returnPopup,
  //   returnVideo: !!returnVideo,
  //   continueBtn: !!continueBtn,
  //   restartBtn: !!restartBtn
  // });

  // Загружаем сохраненное время видео из localStorage
  let savedVideoTime = 0;
  
  function loadSavedTime() {
    const savedTimeFromStorage = localStorage.getItem('savedVideoTime');
    if (savedTimeFromStorage) {
      savedVideoTime = parseFloat(savedTimeFromStorage);
    }
  }
  
  // Загружаем время при инициализации
  loadSavedTime();

  function showReturnPopup() {
    // console.log('🎬 showReturnPopup called');
    // console.log('🎬 returnPopup element:', returnPopup);
    
    // Загружаем актуальное время перед показом
    loadSavedTime();
    
    // Проверяем, не открыт ли основной VSL
    const mainPopup = document.getElementById('popup');
    if (mainPopup && mainPopup.style.display === 'flex') {
      // console.log('🎬 Main popup is open, returning');
      return;
    }
    
    // Останавливаем VSL в чате
    if (window.stopChatVsl) {
      window.stopChatVsl();
    }
    
    // Сначала показываем попап
    if (returnPopup) {
  // console.log('🎬 Showing returnPopup');
  // console.log('🎬 returnPopup current display:', returnPopup.style.display);
      
      // Принудительно обновляем стили
      returnPopup.style.display = 'flex';
      returnPopup.style.visibility = 'visible';
      returnPopup.style.opacity = '1';
      returnPopup.style.zIndex = '1001';
      
  // console.log('🎬 returnPopup display set to flex');
  // console.log('🎬 returnPopup styles updated');
      
      // Проверяем, что попап действительно видим
      setTimeout(() => {
        const computedStyle = window.getComputedStyle(returnPopup);
  // console.log('🎬 returnPopup computed display:', computedStyle.display);
  // console.log('🎬 returnPopup computed visibility:', computedStyle.visibility);
  // console.log('🎬 returnPopup computed opacity:', computedStyle.opacity);
  // console.log('🎬 returnPopup computed z-index:', computedStyle.zIndex);
        
        // Дополнительная проверка видимости
        const rect = returnPopup.getBoundingClientRect();
  // console.log('🎬 returnPopup bounding rect:', rect);
  // console.log('🎬 returnPopup is visible:', rect.width > 0 && rect.height > 0);
        
        // Проверяем, не перекрывается ли попап другими элементами
        const elementsAtPoint = document.elementsFromPoint(rect.left + rect.width / 2, rect.top + rect.height / 2);
  // console.log('🎬 Elements at popup center:', elementsAtPoint);
        const isOnTop = elementsAtPoint[0] === returnPopup;
  // console.log('🎬 Popup is on top:', isOnTop);
      }, 100);
      
      // ПОКАЗЫВАЕМ ТЕКСТОВЫЙ ОВЕРЛЕЙ С КНОПКАМИ ПРИ ОТКРЫТИИ ПОПАПА
      const textOverlay = returnPopup.querySelector('.vsl-text-overlay');
  // console.log('🎬 textOverlay element:', textOverlay);
      if (textOverlay) {
        textOverlay.style.display = 'flex';
  // console.log('🎬 textOverlay shown');
      } else {
  // console.error('❌ textOverlay not found!');
      }
    } else {
  // console.error('❌ returnPopup element not found!');
    }
    
    if (returnVideo) {
      // Устанавливаем время видео с сохраненного момента
      returnVideo.currentTime = savedVideoTime;
      
      // Запускаем автовоспроизведение с обходом блокировки
      startAutoplayWithBypass(returnVideo);
      
      // Настраиваем попап на определенной минуте для этого видео
      if (window.setupVslTimePopup) {
        window.setupVslTimePopup(returnVideo);
      }

      // Открываем форму заказа когда видео закончилось
      returnVideo.addEventListener('ended', function() {
        if (window.showOrderFormModal) {
          window.showOrderFormModal();
        }
        // Запускаем таймеры при показе формы
        if (window.startGlobalTimers) {
          window.startGlobalTimers();
        }
      });
    }
  }

  function hideReturnPopup() {
  // console.log('🔒 hideReturnPopup called');
    
    // Сохраняем текущее время видео перед закрытием
    if (returnVideo) {
      saveVideoTime();
    }
    
    if (returnPopup) {
  // console.log('🔒 Hiding returnPopup');
      returnPopup.style.display = 'none';
    } else {
  // console.error('❌ returnPopup element not found in hideReturnPopup');
    }
    
    if (returnVideo) {
      returnVideo.pause();
    }
    
    // НЕ запускаем VSL в чате после закрытия попапа
    // VSL в чате должен запускаться только при клике на кнопку play
  }

  function saveVideoTime() {
    if (returnVideo) {
      savedVideoTime = returnVideo.currentTime;
      
      // Проверяем время всех плееров и сохраняем самое позднее
      const chatVideo = document.getElementById('vslVideoInline');
      const fullscreenVideo = document.getElementById('fullscreenVideo');
      const returnVisitVideo = document.getElementById('returnVisitVideo');
      
      let maxTime = returnVideo.currentTime;
      let maxSource = 'return';
      
      if (chatVideo && chatVideo.currentTime > maxTime) {
        maxTime = chatVideo.currentTime;
        maxSource = 'chat';
      }
      
      if (fullscreenVideo && fullscreenVideo.currentTime > maxTime) {
        maxTime = fullscreenVideo.currentTime;
        maxSource = 'fullscreen';
      }
      
      if (returnVisitVideo && returnVisitVideo.currentTime > maxTime) {
        maxTime = returnVisitVideo.currentTime;
        maxSource = 'returnVisit';
      }
      
      localStorage.setItem('savedVideoTime', maxTime.toString());
  // console.log('💾 ReturnPopup: Saved MAX video time from', maxSource, ':', maxTime, 'seconds');
    }
  }

  // Функция показа попапа на определенной минуте VSL
  function showReturnVslMidPopup() {
    const popup = document.getElementById('returnVslMidPopup');
    if (popup) {
      popup.style.display = 'block';
      popup.style.opacity = '1';
      popup.style.visibility = 'visible';
      popup.style.zIndex = '1000';
      
      const banner = popup.querySelector('.return-vsl-mid-banner');
      if (banner) {
        banner.style.zIndex = '1001';
      }
    }
  }



  // Функция показа формы заказа
  function showOrderFormModal() {
    const orderFormModal = document.getElementById('orderFormModal');
    if (orderFormModal) {
      orderFormModal.style.display = 'flex';
      setTimeout(() => {
        orderFormModal.classList.add('show');
        // Запускаем таймеры при показе формы
        if (window.startGlobalTimers) {
          window.startGlobalTimers();
        }
      }, 10);
    }
  }



  // Обработчик для крестика в ReturnPopup
  const returnVslCloseBtn = document.getElementById('returnVslCloseBtn');
  if (returnVslCloseBtn) {
  // console.log('✅ Return popup close button found and handler attached');
    
    returnVslCloseBtn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
  // console.log('❌ Return popup close button clicked');
      
      // Показываем попап выбора, если функция доступна
      if (window.showVslCloseChoicePopup) {
  // console.log('🎯 Calling showVslCloseChoicePopup');
        window.showVslCloseChoicePopup();
      } else {
        // Если функция недоступна, просто закрываем попап
  // console.log('⚠️ showVslCloseChoicePopup not available, closing popup directly');
        hideReturnPopup();
      }
    });
  } else {
  // console.error('❌ Return popup close button not found!');
  }



  // Функция для обхода блокировки автовоспроизведения
  function startAutoplayWithBypass(video) {
    if (!video) return;
    
    // Добавляем улучшенные обработчики событий для мобильных устройств
    setupMobileVideoHandlers(video);
    
    video.muted = true;
    video.play().then(() => {
      video.muted = false;
    }).catch((error) => {
      setTimeout(() => {
        video.muted = true;
        video.play().then(() => {
          video.muted = false;
        }).catch((error2) => {
          setupUserInteractionAutoplay(video);
        });
      }, 500);
    });
  }

  // Функция для настройки обработчиков событий видео для мобильных устройств
  function setupMobileVideoHandlers(video) {
    if (!video) return;
    
    let isStalled = false;
    let stallTimeout = null;
    let retryCount = 0;
    const maxRetries = 3;
    
    // Обработка задержки буферизации
    video.addEventListener('waiting', function() {
  // console.log('⏳ Return video is waiting for data...');
      isStalled = true;
      
      if (stallTimeout) {
        clearTimeout(stallTimeout);
      }
      
      stallTimeout = setTimeout(() => {
        if (isStalled && retryCount < maxRetries) {
  // console.log('🔄 Return video: attempting to recover from stall...');
          retryCount++;
          
          const currentTime = video.currentTime;
          video.load();
          video.currentTime = currentTime;
          
          if (!video.paused) {
            video.play().catch(error => {
  // console.log('❌ Return video: failed to recover from stall:', error.name);
            });
          }
        }
      }, 3000);
    });
    
    // Обработка восстановления после задержки
    video.addEventListener('canplay', function() {
      if (isStalled) {
  // console.log('✅ Return video recovered from stall');
        isStalled = false;
        retryCount = 0;
        
        if (stallTimeout) {
          clearTimeout(stallTimeout);
          stallTimeout = null;
        }
      }
    });
    
    // Обработка приостановки загрузки
    video.addEventListener('suspend', function() {
  // console.log('⏸️ Return video loading suspended');
      setTimeout(() => {
        if (video.paused && !video.ended) {
          video.play().catch(error => {
  // console.log('❌ Return video: failed to resume after suspend:', error.name);
          });
        }
      }, 1000);
    });
    
    // Обработка ошибок
    video.addEventListener('error', function(e) {
  // console.log('❌ Return video error:', e);
      if (retryCount < maxRetries) {
        retryCount++;
  // console.log(`🔄 Return video: retrying playback (attempt ${retryCount}/${maxRetries})...`);
        
        setTimeout(() => {
          const currentTime = video.currentTime;
          video.load();
          video.currentTime = currentTime;
          
          if (!video.paused) {
            video.play().catch(error => {
  // console.log('❌ Return video: retry failed:', error.name);
            });
          }
        }, 2000);
      }
    });
    
    // Обработка потери фокуса страницы
    if (typeof document.hidden !== 'undefined') {
      document.addEventListener('visibilitychange', function() {
        if (document.hidden) {
  // console.log('👁️ Page hidden, pausing return video');
          if (!video.paused) {
            video.pause();
          }
        } else {
  // console.log('👁️ Page visible, resuming return video');
          if (video.paused && !video.ended) {
            setTimeout(() => {
              video.play().catch(error => {
  // console.log('❌ Return video: failed to resume after page visible:', error.name);
              });
            }, 500);
          }
        }
      });
    }
    
    // Обработка потери фокуса окна
    window.addEventListener('focus', function() {
      if (document.hasFocus()) {
  // console.log('🎯 Window focused, checking return video state');
        if (video.paused && !video.ended && !isStalled) {
          setTimeout(() => {
            video.play().catch(error => {
  // console.log('❌ Return video: failed to resume after focus:', error.name);
            });
          }, 300);
        }
      }
    });
    
    // Специальная обработка для iOS Safari
    if (/iPad|iPhone|iPod/.test(navigator.userAgent) && /Safari/.test(navigator.userAgent)) {
  // console.log('🍎 iOS Safari detected for return video, applying special handlers');
      
      let wasPlayingBeforeLock = false;
      
      document.addEventListener('visibilitychange', function() {
        if (document.hidden) {
          wasPlayingBeforeLock = !video.paused;
        } else {
          if (wasPlayingBeforeLock) {
  // console.log('🔓 iOS: Screen unlocked, resuming return video');
            setTimeout(() => {
              video.play().catch(error => {
  // console.log('❌ iOS: Failed to resume return video after unlock:', error.name);
              });
            }, 1000);
            wasPlayingBeforeLock = false;
          }
        }
      });
      
      window.addEventListener('blur', function() {
        wasPlayingBeforeLock = !video.paused;
      });
      
      window.addEventListener('focus', function() {
        if (wasPlayingBeforeLock) {
  // console.log('📱 iOS: App focused, resuming return video');
          setTimeout(() => {
            video.play().catch(error => {
  // console.log('❌ iOS: Failed to resume return video after app focus:', error.name);
            });
          }, 500);
          wasPlayingBeforeLock = false;
        }
      });
    }
  }

  // Функция для запуска видео после взаимодействия пользователя
  function setupUserInteractionAutoplay(video) {
    let hasUserInteracted = false;
    
    const playButton = document.createElement('div');
    playButton.style.cssText = `
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: transparent;
      cursor: pointer;
      z-index: 1000;
    `;
    playButton.innerHTML = `
      <div style="
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 20px 40px;
        border-radius: 10px;
        font-size: 18px;
        text-align: center;
        pointer-events: none;
      ">
        ▶️ Нажмите для воспроизведения
      </div>
    `;
    
    playButton.onclick = () => {
      if (!hasUserInteracted) {
        hasUserInteracted = true;
        playButton.remove();
        video.muted = false;
        video.play();
      }
    };
    
    const interactionEvents = ['click', 'touchstart', 'keydown', 'mousedown'];
    interactionEvents.forEach(event => {
      playButton.addEventListener(event, () => {
        if (!hasUserInteracted) {
          hasUserInteracted = true;
          playButton.remove();
          video.muted = false;
          video.play();
        }
      });
    });
    
    returnPopup.appendChild(playButton);
  }

  // Экспортируем функции для использования в других компонентах
  window.showReturnPopup = showReturnPopup;
  window.hideReturnPopup = hideReturnPopup;
  window.saveVideoTime = saveVideoTime;
  window.showReturnVslMidPopup = showReturnVslMidPopup;
  window.showOrderFormModal = showOrderFormModal;
  
  // Ждем, пока функция showVslCloseChoicePopup станет доступной
  function waitForVslCloseChoicePopup() {
    if (window.showVslCloseChoicePopup) {
  // console.log('✅ showVslCloseChoicePopup is now available');
      return;
    }
    
    setTimeout(waitForVslCloseChoicePopup, 100);
  }
  
  // Запускаем ожидание
  waitForVslCloseChoicePopup();
  
  // Тестовая функция для проверки попапа
  window.testReturnPopup = function() {
  // console.log('🧪 Testing ReturnPopup...');
  // console.log('🧪 returnPopup element:', returnPopup);
  // console.log('🧪 returnVideo element:', returnVideo);
  // console.log('🧪 returnVslCloseBtn element:', returnVslCloseBtn);
    
    if (returnPopup) {
      const computedStyle = window.getComputedStyle(returnPopup);
      // console.log('🧪 returnPopup computed styles:', {
      //   display: computedStyle.display,
      //   visibility: computedStyle.visibility,
      //   opacity: computedStyle.opacity,
      //   zIndex: computedStyle.zIndex,
      //   position: computedStyle.position
      // });
    }
    
    // Пытаемся показать попап
    showReturnPopup();
  };
  
  // console.log('🎬 ReturnPopup initialized. Use window.testReturnPopup() to test.');





  if (continueBtn) {
    continueBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      
      // Сохраняем время
      if (returnVideo) {
        saveVideoTime();
      }
      
      // СКРЫВАЕМ ВЕСЬ ТЕКСТОВЫЙ ОВЕРЛЕЙ С КНОПКАМИ
      const textOverlay = document.querySelector('#returnPopup .vsl-text-overlay');
      if (textOverlay) {
        textOverlay.style.display = 'none';
      }
      
      // Возобновляем видео
      if (returnVideo && returnVideo.paused) {
        returnVideo.play();
      }
    });
  }

  if (restartBtn) {
    restartBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      
      if (returnVideo) {
        // Сбрасываем время видео на начало
        returnVideo.currentTime = 0;
        
        // Очищаем сохраненное время
        savedVideoTime = 0;
        localStorage.removeItem('savedVideoTime');
        
        // Запускаем воспроизведение с начала
        startAutoplayWithBypass(returnVideo);
        
        // Настраиваем попап на определенной минуте
        if (window.setupVslTimePopup) {
          window.setupVslTimePopup(returnVideo);
        }
      }
      
      // СКРЫВАЕМ ВЕСЬ ТЕКСТОВЫЙ ОВЕРЛЕЙ С КНОПКАМИ
      const textOverlay = document.querySelector('#returnPopup .vsl-text-overlay');
      if (textOverlay) {
        textOverlay.style.display = 'none';
      }
    });
  }



  returnPopup?.addEventListener('click', (e) => {
    if (e.target === returnPopup) {
      // Сохраняем время перед закрытием попапа
      if (returnVideo) {
        saveVideoTime();
      }
      
      hideReturnPopup();
      // Показываем домонетку (пятая стадия)
      if (window.showDomonetkaForm) {
        window.showDomonetkaForm();
      }
    }
  });

  // Обработчик клика на видео - просто сохраняем время, НЕ показываем домонетку
  returnVideo?.addEventListener('click', () => {
    // Сохраняем время при клике на видео
    if (returnVideo) {
      saveVideoTime();
    }
    
    // НЕ показываем домонетку при клике на видео
    // Домонетка показывается только при попытке закрыть попап (клик на фон)
  });

  // Настройки видео (перемотка разрешена)
  if (returnVideo) {
    // Добавляем controls для всех устройств
    returnVideo.controls = true;
    returnVideo.setAttribute('controlsList', 'nodownload nofullscreen noremoteplayback');
    
    returnVideo.addEventListener('contextmenu', (e) => {
      e.preventDefault();
      return false;
    });
    
    // console.log('✅ Video seeking enabled for returnVideo');
  }

  // Сохраняем время видео при попытке ухода со страницы
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      saveVideoTime();
    }
  });

  // Периодически сохраняем время видео каждые 10 секунд во время воспроизведения
  setInterval(() => {
    if (returnVideo && !returnVideo.paused && returnVideo.currentTime > 0) {
      saveVideoTime();
    }
  }, 10000); // Каждые 10 секунд

  // Показываем попап при попытке ухода (только если VSL уже был показан)
  let hasShownVsl = false;
  
  // Отслеживаем показ основного VSL
  window.addEventListener('vsl-opened', () => {
    hasShownVsl = true;
  });
  
  document.addEventListener('click', (e) => {
    if (e.target && e.target.closest('#popup')) {
      hasShownVsl = true;
    }
  });
  
  // Exit intent отключен - попап при попытке закрыть страницу не показывается
  // document.addEventListener('mouseleave', (e) => {
  //   if (e.clientY <= 0 && hasShownVsl) {
  //     showReturnPopup();
  //   }
  // });
  

</script>

<style>
  .return-popup-backdrop {
    position: fixed;
    inset: 0;
    background: #000;
    display: none;
    z-index: 1001; /* Выше чем fullscreen-player (z-index: 1000) */
  }

  .return-popup {
    position: fixed;
    inset: 0;
    background: #000;
    width: 100vw;
    height: 100vh;
  }

  .vsl-container {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-overlay {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .vsl-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 2;
  }

  .vsl-overlay video {
    width: 100%;
    height: 100%;
    object-fit: contain; /* Показываем видео полностью без обрезания */
    background: #000; /* Черный фон для пустых областей */
  }

  .vsl-text-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 20px;
    background: rgba(0, 0, 0, 0.4);
    z-index: 3;
  }

  .popup-text {
    text-align: center;
    margin-bottom: 20px;
  }

  .popup-text h3 {
    margin: 0 0 8px 0;
    font-size: 20px;
    font-weight: 700;
    color: #ff4444;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-text p {
    margin: 0 0 6px 0;
    font-size: 14px;
    line-height: 1.3;
    color: #fff;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .consequence {
    font-weight: 600;
    color: #ff4444;
  }

  .solution {
    font-weight: 600;
    color: #fff;
    font-size: 16px;
  }

  .highlight {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 16px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .urgency {
    font-weight: 700;
    color: #ff4444 !important;
    font-size: 14px !important;
    text-transform: uppercase;
    margin: 8px 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .popup-buttons {
    display: flex;
    flex-direction: column;
    gap: 12px;
    width: 100%;
    max-width: 250px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #ff6b35, #ff4444);
    color: white;
    border: none;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    box-shadow: 0 4px 12px rgba(255, 107, 53, 0.4);
    text-transform: uppercase;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #e55a2b, #e53935);
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(255, 107, 53, 0.6);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.9);
    color: #333;
    border: 2px solid rgba(255, 255, 255, 0.3);
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s;
    backdrop-filter: blur(10px);
    text-transform: uppercase;
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 1);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
  }



  @media (max-width: 768px) {
    .vsl-text-overlay {
      padding: 15px;
    }

    .popup-text h3 {
      font-size: 18px;
    }

    .popup-text p {
      font-size: 12px;
    }

    .highlight {
      font-size: 14px !important;
    }

    .urgency {
      font-size: 12px !important;
    }

    .popup-buttons {
      max-width: 200px;
    }

    .btn-primary,
    .btn-secondary {
      padding: 10px 16px;
      font-size: 12px;
    }
  }

  /* Стили для попапа на определенной минуте VSL в ReturnPopup */
  .return-vsl-mid-popup {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
    pointer-events: none; /* Позволяет кликать через попап на видео */
    background: transparent; /* Убираем затемнение видео */
  }

  .return-vsl-mid-banner {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    background: #4CAF50; /* Менее яркий зеленый цвет */
    color: white;
    padding: 15px;
    text-align: center;
    font-weight: bold;
    font-size: 16px;
    z-index: 11;
    pointer-events: auto; /* Позволяет взаимодействовать с баннером */
    position: relative;
  }

  .return-vsl-mid-vignette {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.3) 100%);
    pointer-events: none;
    z-index: 1;
  }

  /* Показываем элементы управления видео для перемотки */
  #returnVslVideo::-webkit-media-controls-timeline,
  #returnVslVideo::-webkit-media-controls-seek-back-button,
  #returnVslVideo::-webkit-media-controls-seek-forward-button {
    display: block !important;
  }
  
  /* Показываем элементы управления для Firefox */
  #returnVslVideo::-moz-media-controls-timeline,
  #returnVslVideo::-moz-media-controls-seek-back-button,
  #returnVslVideo::-moz-media-controls-seek-forward-button {
    display: block !important;
  }
  
  /* Показываем timeline и кнопки перемотки для мобильных браузеров */
  #returnVslVideo::-webkit-media-controls-timeline,
  #returnVslVideo::-webkit-media-controls-seek-back-button,
  #returnVslVideo::-webkit-media-controls-seek-forward-button {
    display: block !important;
    opacity: 1 !important;
    visibility: visible !important;
    pointer-events: auto !important;
    width: auto !important;
    height: auto !important;
  }
  
  /* Разрешаем взаимодействие с видео */
  .video-wrapper {
    pointer-events: auto !important;
    position: relative !important;
    width: 100% !important;
    height: 100% !important;
  }
  
  /* Дополнительные правила для мобильных устройств */
  @media (max-width: 768px) {
    #returnVslVideo {
      /* Разрешаем взаимодействие с видео на мобильных */
      pointer-events: auto !important;
    }
    
    /* Показываем timeline и кнопки перемотки на мобильных */
    #returnVslVideo::-webkit-media-controls-timeline,
    #returnVslVideo::-webkit-media-controls-seek-back-button,
    #returnVslVideo::-webkit-media-controls-seek-forward-button {
      display: block !important;
      opacity: 1 !important;
      visibility: visible !important;
      pointer-events: auto !important;
    }
    
    .video-wrapper {
      /* Разрешаем взаимодействие с видео */
      overflow: visible !important;
      position: relative !important;
    }
  }
</style>
